# PDF reference documents — large binary files, not version controlled
documents/

# macOS
.DS_Store

# Environment / secrets
.env
.env.local
.env*.local
.envrc

# Node / React (Phase 6 visitor UI)
node_modules/
build/
dist/
.cache/

# Editor
.vscode/
.idea/
